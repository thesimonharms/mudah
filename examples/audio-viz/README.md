# audio-viz

Dogfoods `@mudah-cli/mudah/tui`: a `Column` of title, energy label, `Sparkline`, and a band `List`. Audio is stubbed (sine samples × energy). No Ink.

```sh
npm start          # TTY: Program
npm test           # TestTui snapshot + key path
```
