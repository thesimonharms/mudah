{
  "name": "audio-viz",
  "version": "0.1.0",
  "private": true,
  "description": "Audio visualizer TUI — stubbed energy samples into Sparkline.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "audio-viz": "./bin/audio-viz.js"
  },
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "start": "node bin/audio-viz.js viz",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mudah-cli/mudah": "^0.8.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^7.0.0",
    "vitest": "^4.0.0"
  }
}
