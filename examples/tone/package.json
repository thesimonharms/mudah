{
  "name": "tone",
  "version": "0.5.0",
  "private": true,
  "description": "Streaming sine + one-shot blip — Mudah + @mudah-cli/audio.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "tone": "./bin/tone.js"
  },
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "start": "node bin/tone.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mudah-cli/audio": "^0.8.0",
    "@mudah-cli/mudah": "^0.8.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^7.0.0",
    "vitest": "^4.0.0"
  }
}
