{
  "name": "melody",
  "version": "0.5.0",
  "private": true,
  "description": "Public-domain melodies through the OS mixer — Mudah + @mudah-cli/audio.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "melody": "./bin/melody.js"
  },
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "start": "node bin/melody.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mudah-cli/audio": "^0.5.1",
    "@mudah-cli/mudah": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^7.0.0",
    "vitest": "^4.0.0"
  }
}
